import { SearchByBreedUseCase } from './search-by-breed-usecase';

describe('SearchByBreedUseCase', () => {
	it('should create an instance', () => {
		// expect(new SearchByBreedUseCase()).toBeTruthy();
	});
});
